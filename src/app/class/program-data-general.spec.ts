import { ProgramDataGeneral } from './program-data-general';

describe('ProgramDataGeneral', () => {
  it('should create an instance', () => {
    expect(new ProgramDataGeneral()).toBeTruthy();
  });
});
